Placeholder. `src/Ucp` becomes a real project in phase 9 (`P9-5`): the
`/.well-known/ucp` manifest derived from a capability registry, with the
transactional capabilities and AP2 following in phase 11. There is no `.csproj`
here yet, and it is not in `Tendero.slnx` on purpose — a project with no code
would be one more assembly for the architecture rules to exempt.
